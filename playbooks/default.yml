---
- hosts: all
  become: yes
  become_user: root
  #gather_facts: no
  roles:
    - { role: ../galaxy-roles/java }
    - { role: ../galaxy-roles/dbmanage }
    - { role: ../galaxy-roles/artifactory }
